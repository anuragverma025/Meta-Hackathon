import sys; import json; from pathlib import Path; ROOT = Path('.').resolve(); sys.path.insert(0, str(ROOT)); from server.environment import EnterpriseEnvironment; from models import EnterpriseAction; env = EnterpriseEnvironment(scenario_path=str(ROOT / 'env/scenarios/scenario_01.yaml')); action = EnterpriseAction(agent_id='it_agent', tool_call='get_tickets', tool_params={}); res = env.step(action); print('TOOL RESULTS:', env.inner_env._last_step_result.info.get('tool_results'))
